{
  "name": "serverless-aws-canary-deployments",
  "version": "0.1.0",
  "description": "",
  "main": "serverless-aws-canary-deployments.js",
  "scripts": {
    "test": "NODE_ENV=test ./node_modules/mocha/bin/mocha $(find ./lib/ -name '*.test.js' -not -path './node_modules/*')",
    "watch": "NODE_ENV=test mocha -w $(find ./lib/ -name '*.test.js' -not -path './node_modules/*')"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "flat": "^4.0.0",
    "lodash": "^4.17.4",
    "omit-empty": "^0.4.1"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "eslint": "^4.18.1",
    "eslint-config-airbnb": "^16.1.0",
    "mocha": "^5.0.0"
  }
}
